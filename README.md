# Sparked
It's like sparky but better

[Discord link to install the bot](https://discord.com/oauth2/authorize?client_id=1424509417067647106) (It only works if the bot is running which is almost never, so you can clone this repo and make your own bot and add the BOT_TOKEN and APP_ID keys in .env for your bot and run it with [Node.JS](https://nodejs.org/), also make sure you have a version new enough that supports type stripping)